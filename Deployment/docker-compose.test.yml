version: '3.8'

services:
  caldo-ui:
    image: noserdocker/caldo-ui:dev
    ports:
      - 8009:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

  caldo-api:
    image: noserdocker/caldo-api:dev
    environment:
      BASE_URL: ${BASE_URL_TEST}
    volumes:
      - ./data:/app/Data
    ports:
      - 80
